<%= form_with(model: history, local: true) do |form| %>
  <% if history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(history.errors.count, "error") %> prohibited this history from being saved:</h2>

      <ul>
      <% history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :owner %>
    <%= form.text_field :owner, id: :history_owner %>
  </div>

  <div class="field">
    <%= form.label :event %>
    <%= form.text_field :event, id: :history_event %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date, id: :history_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
